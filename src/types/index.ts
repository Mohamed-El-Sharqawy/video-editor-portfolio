export interface Creator {
  id: number;
  name: string;
  subscribers: string;
  avatar: string;
}

export interface VideoShort {
  id: number;
  title: string;
  views: string;
  thumbnail: string;
}

export interface ClientVideo {
  id: number;
  title: string;
  thumbnail: string;
}